import React from 'react';
import Base from '../core/Base';

const Profile = () => {
   return (
      <Base title='Profile Page'>
         <h1> Profile </h1>
      </Base>
   );
};

export default Profile;
